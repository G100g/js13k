{
  "name": "2022",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "check:bundle": "node ./scripts/check-file-size.js",
    "zip": "npm run build && node ./scripts/zip.js",
    "party": "npm run zip && npm run check:bundle"
  },
  "devDependencies": {
    "archiver": "^5.3.1",
    "chalk": "^5.0.1",
    "typescript": "^4.6.4",
    "vite": "^3.0.7"
  },
  "dependencies": {
    "kontra": "^8.0.0"
  }
}
